docs/structure.md contains the current state model of the Earthbux platform (page-by-page spec, discussion model, endpoints). docs/INSTRUCTIONS.md is the build queue — start at ## BUILD SEQUENCE §0. `docs/token_model.md` is the money model and `docs/ME_OE_FINALIZATION.md` is the dated record of the decisions behind it — read the second one only when you need to know WHY, not WHAT.
