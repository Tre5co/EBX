docs/structure.md contains the current state model of the Earthbux platform (page-by-page spec, discussion model, endpoints) — each page's ▶ NEXT heading is its build order. docs/INSTRUCTIONS.md is the build queue — start at ## BUILD SEQUENCE §0. `docs/INSTRUCTIONS.md` `## THE PLAN` is the plan: the build clock, the mission clock, and what is critical; `## REMOVAL REGISTER` in the same file is what is safe to delete. `docs/money_model.md` is the money model, **rewritten 2026-09-16 (read §0 FIRST — it lists every ruling)**: all donations are final at T+15 (budget day); 10% of every ME stake is final at T and another 10% of every OE stake at T+8; a skim only marks finality and moves no money; the non-final part is withdrawable as cash until T+15; the organization claims 5/16 of the pool on budget day; the 32nds are 3 research · 4 advances · 8 framing release · 17 flexible (Earthbux max 8); grants carry a week id, never a cause; being right is rewarded by deployment order, not multipliers; losers of either election exchange into another mission during framing. Its §12 is what `backend/app/token_model.py` implements and the framing/exchange work list. `docs/mission_model.md` is each mission's process. The doc map is listed at the top of `README.md`; every doc opens with a `## Contents` its body follows, and nothing new becomes a file if it can be a dated section of an existing doc. **There is no frontend build**: `resources/js/ebx_shared.js` is the source (the TypeScript copy was retired 2026-09-16) — edit it directly. Voting bots: `scripts/bots/ebx_bots.py` (README §10).
